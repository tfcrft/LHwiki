# LHwiki 版本迭代

## v0.8.6 — 本机写作保护（2026-08-23）

- 维护期间重新开放完整编辑器，文字编辑、预览和 Markdown 均只在当前浏览器运行，不访问 PostgreSQL。
- 云端保存与正式提交继续停用；原按钮保留，点击后仅写入本机副本并显示维护提示。
- 自动发现并恢复最近的本机草稿；离开含有修改的编辑页前请求浏览器确认，同时建议另存本机文档或 Markdown。
- 此前已成功保存在云端的版本不作修改，维护恢复后仍可重新下载。

## v0.8.5 — 资源保护维护模式（2026-08-22）

- 网站暂时进入只读维护，公开目录、教师索引、搜索和文章阅读保持可用。
- 登入、投稿、云端保存、审核、权限管理和访问统计暂停，所有相关接口在访问 PostgreSQL 前直接返回维护状态。
- 已发布文章及云端记录不作修改；编辑器继续提供浏览器本地自动保存，并建议另存本机文档或导出 Markdown。此前已成功保存在云端的内容不会消失，恢复后可重新下载；预计恢复日期为 2026 年 9 月 7 日。

## v0.8.4 — 省流写作模式（2026-08-22）

- 编辑内容继续即时自动保存在本机，云端仅在用户手动保存或提交时写入。
- 旧版编辑页面在下一次云保存前即被拦截，并在保全本机内容后提示刷新，避免继续唤醒 PostgreSQL。
- 云端保存失败后不再后台重试；已有云端草稿、待审投稿和已发布文章均不改变。

## v0.8.3 — 公共浏览降耗与草稿保存保护（2026-08-22）

- 公共目录、文章和健康检查改用严格白名单内容快照，不再因公开浏览唤醒 PostgreSQL。
- 长时间写作优先保存在本机，云端自动保存改为较长空闲后合并执行；资源繁忙时停止自动重试并保留本机副本。
- 增加单实例 PostgreSQL 请求预算与 503 熔断，防止异常调用持续放大资源消耗。
- 备份配置默认改为开发/发布前手动执行，仅由指定电脑显式启用每日任务。

## v0.8.2 — 免费云资源降耗与异常循环保护（2026-08-22）

- 暂停非必要的访问量统计写入，普通健康检查不再唤醒数据库。
- 公共目录与文章采用六小时缓存、并发去重和精简字段读取，减少冷启动后的重复 SQL 与数据传输。
- 云端草稿在用户停顿后合并保存，失败仅有限次数自动重试；本机恢复副本仍会即时保留。
- 取消浏览器与云函数的重试叠乘，并让日常健康检查保持轻量。

## v0.9.0 — 原生样式与多格式导入（2026-08-29）

- 编辑器新增任务项、提示块、代码块、行内公式与常用行内样式，全部收入统一“插入”命令列表。
- 新增 Markdown、DOCX、LaTeX 和纯文本导入，导入前在浏览器内预检块数、字数与格式降级警告。
- 导入可选追加或替换，并支持标准撤销/重做快捷键；DOCX 只提取受控文字与表格，不运行宏或导入任意 HTML。
- 内容协议升级至 schema v3，前端、共享解析器与 CloudBase 云函数保持同一白名单和安全限制。

## v0.8.1 — 深色模式与 Markdown（2026-08-15）

- 新增跟随系统、浅色和深色三种外观选择，并记住手动偏好。
- 投稿编辑器支持 Markdown 输入与输出，兼容现有标题、列表、表格、公式、分栏和折叠内容。
- Markdown 链接和代码采用安全文本渲染，不改变数据库结构、云端接口或角色权限。
- 补全教师卡片、审核队列、致谢头像、投稿提示和更新日志入口的深色视觉层次。

版本号根据网站正式上线后的主要交付节点补记，用于说明功能演进；日期采用北京时间。

## v0.8.0 — 轻量文档工作台（2026-08-13）

- 编辑器加入 H4、纯文本表格、二/三栏、折叠标题、块级公式和分隔线。
- 用统一可搜索命令面板替代常驻格式按钮，支持顶部/块旁/斜杠入口和中英文别名。
- 表格支持键盘 Tab 导航与行列增删；分栏在窄屏自动堆叠，表格横向滚动限制在自身区域。
- 公式使用无第三方依赖、无外部请求的严格 MathML 渲染并保留 LaTeX 源文本。
- 新增递归内容限制、统计与安全渲染，并阻止旧客户端静默覆盖包含高级块的云端草稿。
- 修复复制分栏或折叠块时嵌套 ID 重复造成的内容错写风险，并在旧页面遇到新版内容时停止无效重试、保留本机副本。
- 新增站内更新日志，以倒序、精简的形式公开记录各版本主要变化。

## v0.7.0 — 协作、编辑与资源优化（2026-08-12）

- 管理员可以校订待审核文章，同时保留原投稿人、审核状态和完整审核流程。
- 修复块编辑器在回车、合并分块和粘贴时跳回页面顶部的问题，并增加一键清空稿件。
- 修复新建段落时焦点误落到不可编辑外层容器、导致 Enter 后退出输入状态的问题。
- 长文换行改为局部插入内容块，避免整篇重绘造成页面瞬间跳顶后再滚回输入位置。
- 长文换行后的页面滚动改为跟随输入焦点：焦点仍可见时保持视口稳定，越出可视区域时仅做最小距离的即时滚动。
- 扩充教师索引数据，修正教师姓名与分类；搜索输入不再因重新渲染而丢失焦点。
- 公共浏览改用前端缓存、摘要接口和教师数据按需加载，降低云函数与数据库调用。
- 浏览量保持按有效页面打开累计，但由浏览器批量上报；同一批次重试不会重复计数。
- 普通学号登入不再立即写入用户表，配合分层限流减少自动化扫描造成的资源消耗。
- 增加 GitHub Actions 测试工作流，并补齐 PostgreSQL 迁移、回归测试和开源发布说明。

## v0.6.0 — 教师索引共建（2026-08-09）

- 教师索引新增“补充一位老师”入口，姓名和学科必填，格言选填。
- 教师补充请求进入独立审核流程，批准后才写入正式公开索引。
- 审核中心统一展示文章与教师资料，并明确两类内容的审核边界。
- “我的投稿”新增教师补充状态及审核意见。
- 增加静态教师、已公开教师和待审教师的重复检查，以及账号每日提交上限。
- 新增 PostgreSQL 表、索引、外键、RLS 与服务端角色策略；自动备份覆盖两张新表。

## v0.5.1 — 稳定性修复（2026-08-09）

- 修复 CloudBase PostgreSQL 时间戳在浏览器端触发 `Invalid time value` 的问题。
- 修复运行 API Key/函数初始化故障，避免移动端进入“服务器暂时无法处理请求”。
- 前后端请求增加超时、只读重试、健康检查与错误降级。
- 增加正式域名和备用域名巡检，并配置每日自动稳定性检查。

## v0.5.0 — 写作体验重构（2026-08-09）

- 将编码式正文输入替换为纯文本块编辑器，支持正文、二/三级标题、引用和两类列表。
- 增加 Enter 拆分、块类型快捷键、纯文本粘贴、实时字数和清晰的编辑状态反馈。
- 草稿先保存到本地，再以 revision 乐观锁同步云端；支持离线恢复、失败退避和多页面冲突保护。
- 根据二/三级标题自动生成文章子目录，桌面端固定显示，移动端折叠展示。

## v0.4.0 — 治理、安全与运维（2026-08-08）

- 新增“致谢”页面：固定展示开发者 Chenrx，实名投稿获批后自动加入内容贡献者名单。
- 每个学号只记录第一次实名投稿名称；匿名投稿不进入贡献榜。
- 保护站点管理员 `ray_oriental` 不被降权，并增加已发布文章的持久化编辑与删除。
- 加入请求来源校验、HttpOnly 会话、结构化正文白名单、字段限制、IP 限流和账号日限额。
- 建立自动备份、SHA-256 校验、恢复说明、功能冒烟测试与稳定性检查。

## v0.3.0 — CloudBase 上海迁移（2026-08-07）

- 生产环境从 Cloudflare Workers + D1 迁移到腾讯云 CloudBase 上海。
- 前端使用静态网站托管，后端使用 Node.js 20 HTTP 云函数，数据迁移至 PostgreSQL。
- 配置公开 `/api` 网关路由、服务端数据库访问和生产部署脚本，改善中国大陆访问。
- 保留 Cloudflare 版本作为灾备回退路径。

## v0.2.0 — 校园内容与体验完善（2026-08-07）

- 建立简约、现代的潞河校园视觉风格。
- 加入教师索引、课程与教师评价入口，并补充校园生活、社团、高三备考等目录。
- 登入改为九位学号校内初筛；增加审核者与管理员权限体系。
- 完成搜索、侧栏导航、移动端布局和真诚经验分享式文案。

## v0.1.0 — 最小可用版（2026-08-07）

- 建立公开目录、分区、文章浏览和关键词搜索。
- 实现学生投稿、审核退回/拒绝/批准、批准后公开发布的完整闭环。
- 首版采用 Cloudflare Workers + D1，支持免费部署和基础会话管理。
